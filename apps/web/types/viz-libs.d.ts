/**
 * Upstream visualization libraries ship their own type definitions.
 * This file is intentionally left empty so that the viz-specific tsconfig
 * can include it without duplicating or shadowing vendor typings.
 *
 * Canonical spec aliases live in {@link ../lib/viz/spec-types.ts}.
 */
export {};
